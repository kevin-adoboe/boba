name: PR CI

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop

jobs:
  go-lint-and-test:
    name: Go Lint and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install dependencies
        run: go mod download

      - name: Run go vet
        run: go vet ./...

      - name: Run go build
        run: go build ./...

      - name: Run tests
        run: |
          # Exclude packages that require additional build artifacts or setup
          # op-e2e and op-acceptance-tests require contract artifacts
          # op-validator depends on op-e2e/config which has initialization dependencies
          # op-deployer and op-test-sequencer have environment-dependent tests
          go test $(go list ./... | grep -v '/op-e2e/' | grep -v '/op-acceptance-tests/' | grep -v '/op-validator/' | grep -v '/op-deployer/' | grep -v '/op-test-sequencer/') -short -timeout 5m
